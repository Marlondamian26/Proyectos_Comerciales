export { handlers, auth, signIn, signOut } from "@/lib/auth/auth.config";
export { credentialsAuthorize } from "@/lib/auth/credentials-authorize";
export type { AuthorizeCredentials, AuthorizeResult } from "@/lib/auth/credentials-authorize";
export type { Session } from "next-auth";